{
  "name": "@sked/parse-core",
  "version": "1.0.0",
  "description": "텍스트에서 일정 정보를 추출하는 모듈",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "keywords": [
    "calendar",
    "parser",
    "openai"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.24",
    "eslint": "^8.56.0",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1",
    "zod": "3.24.2"
  },
  "dependencies": {
    "openai": "^4.52.7"
  }
}
